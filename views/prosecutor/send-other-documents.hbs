  {{>breadcrumb}}

  <div class="grid-row">

    <div class="column-two-thirds">

      <h1 class="heading-xlarge">{{pagetitle}}</h1>

      {{#if errors}}
      <div class="error-summary" role="group" aria-labelledby="error-summary-heading" tabindex="-1">

        <h1 class="heading-medium error-summary-heading" id="error-summary-heading">Fix the following</h1>

        <ul class="error-summary-list">
          {{#each errors}}
          <li><a href="#error-documents">{{this.msg}}</a></li>
          {{/each}}
        </ul>

      </div>
      {{/if}}

      <div class="text">

        <p>Put your documents in a zip file and then upload. You can include:</p>

        <ul class="list list-bullet">
          <li><abbr title="Single Justice Procedure">SJP</abbr> notices</li>
          <li>previous convictions</li>
          <li>witness statements</li>
        </ul>

      </div>

      <form class="form" method="post">

        <div class="form-block">
          <div class="panel panel-border-wide" role="note" aria-label="Information">
            <p>If you only want to upload one document, you&rsquo;ll still need to put it in a zip file.</p>
          </div>
        </div>

        <div class="form-group form-group-last{{#if errors}} error {{/if}}"{{#if errors}} id="error-documents"{{/if}}>
          <label for="documents">
            <span class="form-label-bold">Select file</span>
            {{#if errors}}<span class="error-message" id="error-message-documents">You need to upload a file</span>{{/if}}
          </label>
          <input type="file" id="documents" name="documents" accept=".zip"{{#if errors}} aria-describedby="error-message-documents"{{/if}}>
        </div>

        <div class="form-group form-group-related">
          <button class="button" type="submit" id="send" name="send" value="send">Send to court</button>
        </div>

        <p><a href="{{baseurl}}home">Cancel</a></p>

      </form>

    </div>

  </div>
